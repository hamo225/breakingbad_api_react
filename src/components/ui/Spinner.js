import spinner from '../../img/spinner.gif'


const Spinner = () => {
    return (
        <img src={spinner} styel={{width: '100%', margin: '0 auto', display: 'block'}}alt="Loading..."/>
    )
}

export default Spinner
